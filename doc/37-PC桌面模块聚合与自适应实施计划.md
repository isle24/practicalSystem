# PC 桌面模块聚合与自适应实施计划

1. 新增自适应桌面快捷方式网格组件，按容器尺寸和快捷方式数量计算行列与缩放比例。
2. 新增聚合模块内容组件，统一显示子模块并转发打开操作。
3. 在模块定义中增加数据管理、日志审计、资源中心聚合关系，子模块不再直接出现在桌面和启动台。
4. 在桌面快捷方式状态中增加旧模块 ID 到聚合入口 ID 的兼容映射。
5. 保留全局搜索和哈希导航对原子模块的访问。
6. 调整消息未读角标和实习总览指标布局。
7. 编译 PC 前端到 `server/public/pc`，检查常用桌面视口、启动台、聚合入口和原业务窗口。
