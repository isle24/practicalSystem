# 客户端扩展实施计划

1. 在 Tauri 连接页增加多账号连接记录、记住密码和自动登录选项，使用系统凭据库保存密码。
2. 在 PC 桌面网格和启动台增加拖拽排序，本地缓存排序结果，并兼容服务端默认固定模块。
3. 在 PC 日志审计页补充业务动作、模块、操作者和响应结果显示，保留现有分页查询。
4. 将 PC/H5 消息列表整理为按发送人和业务类型分组的会话式时间流，保留已读、未读和关联页面处理。
5. 增加受控 Web 插件目录，提供网盘等外部服务入口，不执行未签名原生插件。
6. 完成本地前端构建、PHP 语法检查、Tauri `cargo check` 和客户端打包前检查后提交并触发 Actions。
