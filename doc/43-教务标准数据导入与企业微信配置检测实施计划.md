# 教务标准数据导入与企业微信配置检测实施计划

**目标：** 在 Webman 项目中实现三种教务 Excel 导入、完整快照与差异确认，同步学生和教师基础档案，并加入企业微信配置检测和“实践系统”默认菜单。

**架构：** 使用独立 `edu` 服务层、源数据表和短期暂存表，由 Redis 队列异步解析，经管理员确认后以事务发布。企业微信配置检测只读调用 `gettoken` 和 `agent/get`，不发布菜单。

**技术栈：** PHP 8.4、Webman 2.1、Laravel Eloquent、PhpSpreadsheet 5.7、Redis 队列、Vue 3、Element Plus。

## 全局约束

- 后端代码位于 `server`，PC 代码位于 `fontend/pc`，H5 不增加导入界面。
- 文档位于 `doc`。
- 只使用 Webman 现有路由、控制器、Service、Model 和队列模式。
- 不创建测试文件，仅执行 PHP 语法、数据库结构、前端构建、接口和实际文件导入验证。
- 学生和教师导入不创建登录账号，企业微信绑定第一次校验成功后再创建。
- 敏感字段不明文存储，不出现在普通列表、操作日志和接口响应中。
- 上传上限为 500MB，具体部署限制需同步 Nginx、PHP 和上游代理。

---

### 任务 1：数据库设施和权限

**文件：**

- 修改：`server/database/init.php`
- 新增：`server/database/migrations/20260917_edu_import.php`

**接口：**

创建 `edu_import_batch`、`edu_student_source`、`edu_teaching_plan_source`、`edu_course_offering_source`、`edu_course_offering_teacher`、`edu_course_offering_class`、`edu_student_stage`、`edu_teaching_plan_stage`、`edu_course_offering_stage`、`edu_import_change`、`edu_import_issue`和 `edu_business_candidate`。字段、状态、外键关系和索引严格按 `doc/42-教务标准数据导入与企业微信配置检测设计.md` 第 4 节实现。

- [x] 在 `schoolBusinessStatements()` 增加所有正式表、不可空的主键、审计字段、软删除字段和 JSON 字段。
- [x] 对大文本 JSON 字段不建立索引，让来源键、批次、状态和范围索引覆盖列表查询。
- [x] 在 `seedMenus()` 和角色菜单数据中增加教务数据管理菜单、四个 `edu:data:*` 权限及 `wechat:proxy:test` 权限。
- [x] 对已有数据库提供可重复执行的列和索引补齐。

**验证：** `php -l server/database/init.php` 、执行初始化后查看 12 张表和所有唯一索引。

### 任务 2：教务数据 Model 和批次仓库

**文件：**

- 新增：`server/app/model/channel/EduDataRecord.php`
- 修改：`server/app/model/channel/TableRecord.php`

**接口：**

`EduDataRecord` 提供以下静态方法：

```php
public static function batchPage(array $filters): array;
public static function batchDetail(int $id): ?array;
public static function createBatch(array $values): int;
public static function updateBatch(int $id, array $values): int;
public static function stageRows(string $type, int $batchId, int $offset, int $limit): array;
public static function sourceRowByKey(string $type, string $sourceKey, bool $lock = false): ?array;
public static function changes(int $batchId, array $filters): array;
public static function issues(int $batchId, array $filters): array;
public static function candidatePage(array $filters): array;
public static function transaction(callable $callback): mixed;
```

- [x] 封装源数据、暂存数据、批次、差异、问题和候选项的查询。
- [x] 所有查询按学校当前动态连接执行，不在 Controller 拼接 SQL。
- [x] 为每类批次提供分页、统计、差异、结果和来源键查询。
- [x] 提供可重复执行的 stage 清理和批次状态更新。

**验证：** `php -l server/app/model/channel/EduDataRecord.php` 和 `php -l server/app/model/channel/TableRecord.php`。

### 任务 3：敏感字段存储与身份校验

**文件：**

- 新增：`server/app/server/edu/EduIdentityCipher.php`
- 新增：`server/app/server/edu/EduIdentityVerifier.php`
- 修改：`server/.env.example`

**接口：**

```php
public function encryptSensitive(array $payload): string;
public function decryptSensitive(string $ciphertext): array;
public function mobileHash(string $mobile): string;
public function identityLastSixHash(string $value): string;
public function verifyStudent(string $studentNum, string $name, ?string $mobile, ?string $identityLastSix): array;
public function verifyTeacher(string $teacherNum, string $name, ?string $mobile = null): array;
```

- [x] 使用 `EDU_IDENTITY_ENCRYPTION_KEY`，密钥缺失时抛出异常。
- [x] 加密负载包含身份证号、家庭地址、出生地和生源地。
- [x] 校验只按学号/工号定位，再比对姓名、手机号 HMAC 或身份证后 6 位 HMAC。
- [x] 返回值仅包含档案 ID 和校验状态，不返回敏感字段。
- [x] 使用学校、学号、企业微信 UserId 和 IP 限流键。

**验证：** 通过 PHP CLI 检查配置缺失时失败，检查正确和错误校验均不返回原始敏感值。

### 任务 4：Excel 模板和分块解析

**文件：**

- 新增：`server/app/server/edu/EduTemplateService.php`
- 新增：`server/app/server/edu/EduSpreadsheetReader.php`
- 修改：`server/app/server/file/FileService.php`
- 修改：`server/config/server.php`

**接口：**

```php
public function template(string $type): array;
public function validateUpload(UploadFile $file, string $type): array;
public function readHeader(string $path, string $type): array;
public function readChunk(string $path, string $type, int $startRow, int $chunkSize): iterable;
```

- [x] 按三个原始文件生成 44、45、91 个表头的模板。
- [x] 模板第一张工作表仅保留原始表头，第二张工作表为“填写说明”。
- [x] 导入只读取 `sheet1`，表头必须精确匹配名称和顺序。
- [x] 将 `max_package_size` 和教务文件类型上限调整为 500MB。
- [x] 校验 xlsx 解压体积和压缩比，仅允许 xls/xlsx，限制单类型行数。
- [x] 使用 PhpSpreadsheet 读取器和分块 ReadFilter，不一次性将全文件行加载到内存。

**验证：** 分别下载三个模板，检查表头顺序和说明工作表；使用实际文件检查分块解析。

### 任务 5：导入 Service 和快照合并

**文件：**

- 新增：`server/app/server/edu/EduImportService.php`
- 新增：`server/app/server/edu/EduStudentSyncService.php`
- 新增：`server/app/server/edu/EduTeacherSyncService.php`
- 新增：`server/app/server/edu/EduBusinessCandidateService.php`

**接口：**

```php
public function upload(Request $request, string $type): array;
public function parse(int $batchId): array;
public function publish(int $batchId, int $accountId): array;
public function cancel(int $batchId, int $accountId): array;
public function classifyCandidate(int $id, string $businessType, int $accountId): array;
public function confirmCandidates(array $ids, int $accountId): array;
```

- [x] 实现文件去重、类型和学年学期互斥。
- [x] 以学号、复合计划来源键和教学班 ID 进行幂等合并。
- [x] 按完整快照标记缺失数据，不直接删除。
- [x] 学生同步并保留学院、年级、专业、班级映射，演示基础档案无法匹配时停用。
- [x] 教师按教工号同步，主讲/辅讲保存到关系表。
- [x] 按名称、学时和标记生成实习、实验、实训、社会实践候选项。
- [x] 源数据发布与基础档案同步分步进行，实现 `partial_failed` 重试。

**验证：** 通过三个实际文件核对新增、更新、缺失、停用和分类候选统计。

### 任务 6：Redis 导入 Consumer

**文件：**

- 新增：`server/app/queue/redis/EduImportConsumer.php`
- 新增消费者后由 `server/config/plugin/webman/redis-queue/process.php` 现有 `consumer_dir` 自动加载，无需改动 `CronTask.php`

**接口：**

```php
public function consume(array $payload): void;
```

- [x] 队列消息只传批次 ID、学校数据库 ID 和导入类型，不传敏感原文。
- [x] 每批按固定行数分块写入 stage 表，更新 progress。
- [x] 失败最多重试 3 次，超过后标记 failed，不发布源数据。
- [x] 清理超时、失败和取消批次的 stage 数据。

**验证：** 查看队列处理时批次状态、progress 和 stage 行数。

### 任务 7：EduDataController 和接口

**文件：**

- 新增：`server/app/controller/Api/EduDataController.php`

路由使用 Webman 现有控制器自动路由，实际请求统一带 `/api` 前缀，不修改 `server/config/route.php`。

**接口：**

```text
GET  /edu-data/template
POST /edu-data/upload
GET  /edu-data/batches
GET  /edu-data/batch-detail
POST /edu-data/publish
POST /edu-data/cancel
GET  /edu-data/students
GET  /edu-data/teaching-plans
GET  /edu-data/course-offerings
GET  /edu-data/issues
GET  /edu-data/candidates
POST /edu-data/classify-candidate
POST /edu-data/confirm-candidates
```

- [x] 按现有 `Responds` 模式返回统一数据结构和状态码。
- [x] 每个方法检查 `edu:data:*` 权限、学校数据连接和管理范围。
- [x] 上传接口只创建批次并入队，不同步执行大文件解析。
- [x] 发布、取消、分类和确认候选项使用工作流锁和操作日志。

**验证：** 执行 PHP 语法检查，通过实际文件请求核对批次、错误和差异响应。

### 任务 8：企业微信配置检测与默认菜单

**文件：**

- 修改：`server/app/controller/Api/WechatController.php`
- 修改：`server/app/server/wechat/WechatClient.php`
- 修改：`fontend/pc/src/App.vue`
- 修改：`fontend/pc/src/api/system.js`

**接口：**

```php
public function checkConfig(Request $request): Response;
public function token(string $corpId, string $secret): array;
public function agent(string $accessToken, string $agentId): array;
```

- [x] `checkConfig` 接口使用当前表单值调用 `gettoken` 和 `agent/get`。
- [x] 返回应用名称、AgentId 和检测时间，不写入 token 和 Secret。
- [x] 保存密钥返回掩码，掩码传入表示保持原密钥。
- [x] 菜单为空时生成“实践系统”一级 `view` 菜单，指向项目根地址。
- [x] 根地址采用学校请求域名和反向代理协议，本地地址或非 HTTPS 显示警告。
- [x] 检测按钮不调用菜单发布接口。

**验证：** 在不修改菜单的前提下检查成功、凭证错误、超时和代理请求。

### 任务 9：PC 教务数据界面

**文件：**

- 新增：`fontend/pc/src/components/EduDataPanel.vue`
- 修改：`fontend/pc/src/App.vue`
- 修改：`fontend/pc/src/api/system.js`
- 修改：`fontend/pc/src/styles.css`

- [x] 在“数据管理”下增加“教务数据”入口。
- [x] 页面提供三个模板下载按钮和三个文件上传入口。
- [x] 以页签显示批次、学生数据、开课计划、开课情况、业务候选和导入问题。
- [x] 展示批次进度、差异类型、问题行号和候选分类。
- [x] 教务数据只能由超级管理员和学校管理员上传、发布和确认。
- [x] 所有敏感字段显示为脱敏值。

**验证：** `npm --prefix fontend/pc run build`，在桌面宽度下检查页面跳转、上传状态、差异和候选操作。

### 任务 10：首次数据导入与实际核对

**文件：**

- 修改：`doc/42-教务标准数据导入与企业微信配置检测设计.md`（记录实际导入结果）

- [x] 在 `practical_default` 导入开课计划，等待完成后发布。
- [x] 导入在校学生，等待完成后发布学生和基础档案。
- [x] 导入开课情况，等待完成后发布教师和教学班。（5,088 条教学班、1,429 名教师）
- [x] 核对 33,418 名学生、共 3,591 条计划、共 5,088 条教学班和 1,429 名教师。（四项全部吻合）
- [x] 实际核对学号、教工号、教学班 ID 重复、差异、问题和候选数量。（重复 0、问题 0、候选 1,462）
- [x] 保留演示业务数据，停用无法匹配的演示基础档案。（保留 dep 1 及其 class 1，停用 dep 2/4/12）
- [x] 不自动确认业务候选项。

**验证：** 实际导入完成后查询批次、源数据和基础档案，确认历史演示业务不被删除。

### 任务 11：交付构建与运行验证

- [x] 执行本计划所有新增和修改 PHP 文件的 `php -l`。
- [x] 执行 `php server/database/init.php`，确认表和索引可重复补齐。
- [x] 执行 `npm --prefix fontend/pc run build`，同步 `server/public/pc`。
- [x] 执行 `curl -I http://127.0.0.1:8787/pc/index.html` 和 `curl -I http://127.0.0.1:8787/h5/index.html`。
- [x] 使用实际文件调用上传、批次、差异、发布、企业微信配置检测接口。
- [x] 检查 Git 工作区只包含本功能文件、文档和前端构建产物。

## 交付顺序

1. 任务 1–4：数据库、Model、敏感存储和模板解析。
2. 任务 5–7：导入 Service、队列和接口。
3. 任务 8：企业微信配置检测。
4. 任务 9：PC 前端页面。
5. 任务 10–11：首次导入、核对、构建和交付。

### 任务 12：开课情况发布缺陷修复（补充）

- [x] 修正 `diff()` 对数组字段的字符串转换异常。
- [x] 修正 `upsertSource()` 中 `teachers` 判空条件恒为假导致关系表不写入。
- [x] 补充 `edu_course_offering_class` 教学班组成拆分写入。
- [x] 暂存数据缺失时 `publish()` 返回 409，不再空循环成功。
- [x] 发布失败回退 `pending_confirm` 并保留暂存数据。

### 任务 13：基础档案重复修正（补充）

- [x] 修正学院、年级、专业、开课学院四处查找逻辑的“代码与名称二选一”缺陷。
- [x] 合并存量重复行：学院 31→17，年级 7→6，专业 1,287→412。
- [x] 回填开课情况中 697 条 `dep_id` 为空的教学班。
- [x] 重新映射教师部门，315 人因来源部门不在学院档案而保留空值。
- [x] 以修正后逻辑复算 500 条已有学生档案，四张表均无新增行。

### 任务 14：验收问题修正（补充）

首轮验收未通过，按验收意见逐项修正。

- [x] `SENSITIVE_STUDENT_FIELDS` 由 6 项扩展为 10 项，剥离逻辑与常量共用同一来源。
- [x] `profession()` 改以 `(dep_id, grade_id, profession_code)` 为键，不再回写 `grade_id`。
- [x] `department()`、`grade()`、`departmentId()` 改为名称或代码任一命中，并在缺代码时回填。
- [x] 新增 `resolveClassId()`，依次尝试 `class_num`、`class_name`、`class_short_name`。
- [x] 暂存表新增 `change_type`、`before_json`、`diff_json`、`classification_source`、`classification_reason` 五列及批次联合索引。
- [x] 差异与分类结果在解析阶段预计算，发布阶段直接取用。
- [x] `candidateStatus()` 兼顾标志列命中，修正 299 行待分类候选项被丢弃。
- [x] 发布改为分块事务并支持断点续传，废弃整批单事务方案。
- [x] 发布锁 TTL 由 15 秒调整为 1,800 秒。
- [x] 候选确认：草稿插入与状态更新同事务，已生成候选直接跳过。
- [x] 移除候选确认仅限 `teaching_plan` 来源的限制。
- [x] 新增 `guardPost()`，5 个写接口非 POST 返回 405。
- [x] 失败响应仅在本地调试模式返回文件路径与调用栈，500 类错误对外统一提示。
- [x] 上传扩展名收窄为 `xlsx`。
- [x] `WechatClient` 支持代理覆盖，检测接口传入表单当前值。
- [x] 源数据、候选项、问题三个视图补充分页。
- [x] 新增数据重建脚本 `20260919_edu_rebuild_archive.php`，默认 dry-run。
- [x] 清空并重建基础档案与教务源数据，重新导入三类源文件。
- [x] 全部 PHP 文件通过 `php -l`，PC 前端重新构建。

验收结果见设计文档 14.5 至 14.10 节。
