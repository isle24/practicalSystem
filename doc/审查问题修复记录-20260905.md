# 审查问题修复记录

## 执行约束
- 按已确认的 13 项审查问题修复，不改变审批业务流程。
- 数据查询封装在 Model；复用默认路由，不修改框架及组件源码。
- 不删除现有文件或业务数据，不新增测试文件。

## 执行顺序
- [x] 账号手机号验证及身份切换（PC、H5 入口和本地字段已补齐）。
- [x] 文件读取、删除、关联权限及静态路径保护；业务引用保存前统一校验。
- [x] 事务内建表、资料变更覆盖、草稿可见性。
- [x] 社会实践锁内状态复查、实验实训首次提交互斥。
- [x] 企业评价手机号失效及验证记录关联。
- [x] 鉴定表初始数据、文件生成条件、归档要求一致性。
- [x] PHP 语法、前端编译、服务访问核对；随修复代码提交编译产物。

## 升级要求
- 先执行 `php server/database/migrations/20260905_verified_mobile.php`，再重载 Webman。可追加数据库名，仅迁移指定数据库；默认迁移 `.env` 中的模板库和默认学校库。其他独立数据库需使用对应连接环境逐一执行。
- 新安装仍由 `server/database/init.php` 创建完整结构。本次修复不在业务事务内创建表；旧库缺少业务表时应先按版本升级结构，不能在请求中临时建表。
- 历史手机号不自动视为已验证。同一用户下的账号仍可切换；跨用户的同手机号账号需分别完成短信验证后才可互相切换。
- 短信复用 `SMS_GATEWAY_URL`、`SMS_GATEWAY_TOKEN`，未配置时拒绝发送，不提供默认验证码或验证绕过。
- 原文件地址不变。业务附件和导出文件需登录并通过权限校验；头像、壁纸、登录背景和菜单图标允许公开展示。
- `/api/file/attach`、`/api/file/detach` 不再允许任意关联业务对象，附件修改由相应业务接口处理。秒传只复用当前上传人已持有的文件。

## Nginx 文件保护
使用 Nginx 时不能直接静态暴露 `public/files`，必须将该目录转发至 Webman，保留 Cookie、Authorization 和真实 Host。示例中的上游地址按部署环境调整：

```nginx
location ^~ /files/ {
    proxy_pass http://127.0.0.1:8787;
    proxy_set_header Host $http_host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_cache off;
}
```

同时检查对象存储、CDN 或其他静态别名，禁止绕过上述鉴权路径公开业务附件。已缓存的公开文件需在部署侧失效处理。

## 本地验证
- 本地学校库、模板库已增加 `users.verified_mobile`，迁移未回填历史手机号，未清理数据或文件。
- 163 个 PHP 文件语法检查、PC/H5 编译和差异格式检查通过；编译产物位于 `server/public/pc`、`server/public/h5`，旧构建资源未删除。PC 仍有构建包体积提示，本轮未扩大范围拆包。
- 两个前端入口均返回 200，各入口引用的 5 个 JS/CSS 资源全部可访问。
- 在只读事务内调用资料、变更、企业评价、归档要求和 recording 表检查后，事务层级仍为 1，PDO 事务仍有效，没有被 DDL 隐式提交。
- 超级管理员、学校管理员、学院管理员、专业管理员、教师、学生和企业角色的个人设置与身份列表接口均返回 200。
- 导出文件：学校级管理员返回 200；其他无权账号的详情接口与原静态地址返回 403；匿名访问返回 401。
- 现存头像图片匿名访问返回 200。两个历史登录背景记录的物理文件原本缺失，返回 404；当前学校未配置使用这两个背景。
- PC 个人设置、390×844 手机视口的绑定弹层已核对，按钮完整显示。
- 未新增自动化用例、未提交或审核业务资料、未发送短信；本地未配置短信网关，完整短信链路仍需接入后验证。

## 待确认项
- 核对认证时另发现 JWT 签名密钥硬编码于版本库。本轮未擅自轮换；需确认是否改用 `.env` 并轮换密钥，轮换会使现有登录失效。
