本次更新修复教务数据导入、权限范围和批处理稳定性问题。

- 教务学生敏感信息仅保存加密负载，接口和差异记录不返回邮箱、手机号哈希及敏感密文。
- 教务导入增加批次互斥和队列条件认领，避免重复解析和并发发布。
- 发布中断保留可继续处理的暂存数据，并支持 `partial_failed` 批次重试。
- 修复教务源数据分页、问题处理和角色数据范围控制。
- Excel 导入支持 `.xls` 和 `.xlsx` 文件。
- 企业微信代理配置增加 HTTPS、允许域名、公网解析和响应大小限制校验。
