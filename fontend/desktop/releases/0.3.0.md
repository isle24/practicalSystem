# 实践管理系统客户端 v0.3.0

- 客户端支持按学校和账号保存桌面模块顺序，拖拽排序不写入服务端。
- 客户端账号密码使用系统凭据库保存，自动登录通过一次性本机桥接完成，网页页面不读取明文密码。
- PC 日志审计增加可读操作名称、接口结果、请求参数、设备信息和详情查看。
- PC 消息中心改为左侧分类会话、右侧时间流布局。
- 增加受控插件中心，支持管理员维护 HTTPS Web/OAuth 入口。

服务端升级：执行 `server/database/updates/0.3.0-client-platform.sql`。
